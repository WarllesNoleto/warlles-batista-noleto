// Importa o módulo de animação ao scroll
import initAnimationScroll from "./modules/animacao-scroll.js";

// Importa o módulo do menu mobile
import initMenuMobile from "./modules/menu-mobile.js";

// Importa o módulo de transição do menu
import initMenuTransition from "./modules/menu-transition.js";

// Importa o módulo de scroll suave
import initScrollSuave from "./modules/scroll-suave.js";

// Importa o módulo de navegação por abas
import initTabNav from "./modules/tabnav.js";

// Importa o módulo de digitação
import initTyping from "./modules/typing.js";

// Inicializa a navegação por abas
initTabNav();

// Inicializa o scroll suave
initScrollSuave();

// Inicializa a animação ao scroll
initAnimationScroll();

// Inicializa a digitação
initTyping();

// Inicializa a transição do menu
initMenuTransition();

// Inicializa o menu mobile
initMenuMobile();